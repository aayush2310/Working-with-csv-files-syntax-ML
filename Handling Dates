Whenever a date is passed,it is passed in the form of a string so if we want to perform operations like after which date and before which,we cant do it,so to convert to date format we use it

pd.read_csv('data.csv',parse_dates=['date'])
